#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif
	
	int N, now;

	scanf("%d", &N);

	scanf("%d", now);

	rep(i, 2, N){
		int a;
		scanf("%d", &a);
		if(a > now){
		}
	}

	return 0;
}
