#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int N, M;

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d%d", &N, &M);

	return 0;
}
