#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d

	return 0;
}
