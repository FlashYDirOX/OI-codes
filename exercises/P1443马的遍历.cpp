#include <cstdio>

const int d[8][2] = {{-2, 1}, {-1, 2}, {1, 2,}, {2, 1}, {-2, -1}, {-1, -2}, {1, -2}, {2, -1}};

int main()
{
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);

	int N, M, x1, y1;

	scanf("%d%d%d%d", &N, &M, &x1, &y1);

	while (head < last) {
	}

	return 0;
}
