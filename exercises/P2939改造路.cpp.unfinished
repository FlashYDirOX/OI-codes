#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d%d%d", &N, &M, &K);

	rep(i, 1, M){
		int u, v, w;
		scanf("%d%d%d", &u, &v, &w);
		add_e
	}

	return 0;
}
