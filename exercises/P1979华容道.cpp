#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int N, M, Q;

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d%d%d", &N, &M, &Q);

	return 0;
}
