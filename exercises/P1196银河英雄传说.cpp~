#include <cstdio>
#include <cctype>

#define REP(i, a, b) for(register int i = a; i <= b; i++)

const int MAXN = 30001;

int T;
int father[MAXN], head[MAXN], value[MAXN];

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	char c;

	scanf("%d", &T);

	c = getchar();

	while(T--){
		while(!isdigit(c) && c != 'M' && c != 'C')
			c = getchar();
		if(c == 'M')

		if(c == 'C')
	}

	return 0;
}
