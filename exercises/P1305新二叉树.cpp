#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

int main()
{
#ifndef ONILNE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d", &N);
	
	rep(i, 1, N){
		
	}

	return 0;
}
