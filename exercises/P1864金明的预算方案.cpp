#include <cstdio>

#define rep(i, a, b) for(register int i = a; i <= b; i++)

const int MAXN = 61;

struct node {
	int value, imp, 
}

int N, M;

int main()
{
#ifndef ONLINE_JUDGE
	freopen("in", "r", stdin);
	freopen("out", "w", stdout);
#endif

	scanf("%d%d", &M, &N);

	rep(i, 1, N){
	}

	return 0;
}
